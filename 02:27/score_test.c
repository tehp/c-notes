int is_valid_score(int n) {
  if (0 <= n && n <= 100) {
    return 1
  } else {
    return 0;
  }
}

int is_valid_score(int n) {
  if (0 <= n && n <= 100) {
    return 1;
  }
  return 0;
}

int is_valid_score(int n) {
  return (0<=n && n<=100 ? 1 : 0);
}

int is_valid_score(int n) {
  return 0 <= n && n <= 100;
}
